# Repository working rules

## 对外文档

- `README.md`、接入指南、API Reference 和各示例目录的说明文档面向使用者与二次开发者，只描述稳定的当前行为、使用步骤、接入约束、验收标准和故障排查。
- 不要在对外文档中记录某次提交“新增了什么、修复了什么、改成了什么”，也不要使用“本次修改、已经修复、现已支持、不再”等变更记录口吻。
- 版本差异、修复清单、性能对比和提交历史只应写入 CHANGELOG、release notes 或用户明确指定的版本发布文档；未被要求时不要自行创建变更日志。
- 只有公开 API、配置项、命令、操作流程、兼容性、接入约束或用户可见行为发生变化时，才同步对应使用文档。内部重构、测试补充和不改变使用方式的缺陷修复通常不需要改对外文档。
- API Reference 仅在请求/响应字段、鉴权、错误码、接口语义或兼容性契约变化时修改；已有接口仅新增前端入口时，不重复改写接口定义。
- 提交前检查文档差异：每一段都应能回答“使用者怎么用”或“二次开发必须遵守什么”。如果只能回答“这次代码改了什么”，应删除或移到变更日志。
