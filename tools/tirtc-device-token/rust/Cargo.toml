[package]
name = "tirtc-signing"
version = "1.0.0"
edition = "2021"
description = "TiRTC TGV1-HMAC-SHA256 token signing — Rust"
license = "MIT"

[dependencies]
hex = "0.4"
sha2 = "0.10"
hmac = "0.12"
chrono = { version = "0.4", features = ["clock"] }

[dev-dependencies]
serde_json = "1"
