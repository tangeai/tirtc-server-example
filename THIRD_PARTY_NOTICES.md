# Third-party notices

本仓库源代码采用根目录 `LICENSE` 所述的 MIT License。依赖项和预编译第三方组件继续受各自许可证及服务条款约束。

`thing-connect/device-sim/sdk/` 中的 TiRTC 头文件、静态库和动态库用于设备接入示例。MIT License 不自动授予这些第三方二进制的商标、专利、再分发或商业服务权限。发布产品或重新分发 SDK 前，请根据取得 SDK 的渠道确认适用的 TiRTC 许可和服务条款。

Go 与 npm 依赖的具体版本记录在 `go.mod`、`go.sum` 和各 `package-lock.json` 中。发行方应在发布流程中生成依赖许可证清单，并按对应许可证保留必要声明。
